{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Écran de lancement (main) et interface (cad) : commandes locales, mise à jour, relance, choix de fichier.",
  "windows": [
    "main",
    "cad"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "updater:default",
    "process:allow-restart",
    "dialog:allow-open"
  ]
}
